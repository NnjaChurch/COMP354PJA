/**
 * MessageType enum used to define the types of messages sent from the View to Control
 * @author Kevin McAllister (40031326) - Iteration 1
 */
package control;

public enum MessageType {
    NEW_GAME, NEXT, UNDO, REDO, SELECT
}
